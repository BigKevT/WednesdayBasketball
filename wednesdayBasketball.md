# Wednesday Basketball 報名網站 PRD

## 1. 產品定位

Wednesday Basketball 是一個給固定球友使用的每週籃球報名網站。使用者主要來自既有熟人群組，不對外公開招募。網站目標不是建立完整會員平台，而是讓大家用手機快速查看本週球局狀態、報名、取消，並保留每週歷史紀錄。

第一版產品應該像一個清楚、輕量、有運動感的報名板：打開就知道這週幾個人、還能不能報、誰已經報名。

## 2. MVP 目標

- 球友可以查看下一場固定球局資訊。
- 球友可以只輸入名字完成報名。
- 系統自動區分正式名單與候補名單。
- 正式名額最多 15 人，候補最多 5 人。
- 滿 20 人後關閉報名。
- 所有球友可以查看正式名單與候補名單。
- 每個名字旁邊都有取消按鈕。
- 正式名單有人取消時，候補第一位自動遞補為正式。
- 每週報名截止時間為星期三中午 12:00。
- 每週球局結束後，系統自動切換到下一週場次。
- 管理者可以進入後台調整場次名單與報名狀態。
- 資料庫保存歷史場次與報名紀錄。

## 3. 非目標

第一版暫不處理：

- 使用者登入。
- 手機驗證、LINE 登入或 Email 驗證。
- 線上付款。
- 自動通知。
- 多場次、多地點或公開招募。
- 複雜會員資料、出席率、排行榜。

## 4. 使用者與前提

### 4.1 主要使用者

固定球友，通常會從 LINE 群組或分享連結打開網站。

### 4.2 信任前提

第一版以熟人群組為前提：

- 不需要登入。
- 報名只需要輸入名字。
- 取消按鈕直接顯示在名字旁邊。
- 任何人都可以取消名單上的名字。

若未來發生誤刪或惡意操作，再補上簡單驗證機制。

## 5. 固定球局規格

- 星期：每週三
- 時間：19:30-21:30
- 地點：木柵國中
- 正式名額：15 人
- 候補名額：5 人
- 報名總上限：20 人
- 報名截止：每週三 12:00

系統只顯示「下一場球局」作為主要報名目標。歷史場次留存在資料庫，但第一版不需要提供歷史查詢頁面。

## 6. 核心流程

### 6.1 查看首頁

1. 使用者進入首頁。
2. 首頁顯示下一場球局摘要。
3. 使用者可以看到：
   - 日期
   - 星期
   - 時間
   - 地點
   - 正式名單人數，例如 `12 / 15`
   - 候補人數，例如 `1 / 5`
   - 狀態，例如可報名、候補中、已滿
4. 首頁不顯示完整名單。
5. 使用者點擊「查看 / 報名」進入詳情頁。

### 6.2 查看詳情頁

1. 使用者進入詳情頁。
2. 頁面顯示下一場球局完整資訊。
3. 頁面顯示正式名單與候補名單。
4. 若尚未滿 20 人，顯示報名表單。
5. 若已滿 20 人，隱藏或停用報名表單，顯示「本週已滿」。
6. 若已超過星期三中午 12:00，關閉球友端報名，顯示「本週報名已截止」。

### 6.3 報名

1. 使用者在詳情頁輸入名字。
2. 使用者送出報名。
3. 系統檢查同一場次是否已有相同名字。
4. 若同名已存在，顯示錯誤訊息，不允許重複報名。
5. 若正式名單未滿 15 人，加入正式名單。
6. 若正式名單已滿但候補未滿 5 人，加入候補名單。
7. 若正式與候補都已滿，報名按鈕關閉，使用者不可送出。
8. 若已超過報名截止時間，報名按鈕關閉，使用者不可送出。

### 6.4 取消報名

1. 詳情頁每個名字旁邊都有取消按鈕。
2. 使用者點擊取消。
3. 系統可以顯示簡單確認，例如「確定取消這個報名嗎？」
4. 使用者確認後，該筆報名狀態改為取消。
5. 若被取消者在正式名單，且候補名單有人，候補第一位自動轉為正式名單。
6. 名單與人數即時更新。

### 6.5 管理者調整名單

1. 管理者進入後台。
2. 管理者需要通過簡單權限驗證，例如管理密碼或 admin token。
3. 管理者可以查看下一場球局的正式名單、候補名單與取消名單。
4. 管理者可以手動新增球友到正式名單或候補名單。
5. 管理者可以取消名單中的球友。
6. 管理者可以將候補球友手動調整為正式名單。
7. 管理者可以將正式名單球友調整到候補名單。
8. 管理者調整後，系統需重新計算正式人數與候補人數。
9. 管理者操作需保留資料紀錄，不直接刪除報名資料。

### 6.6 每週自動切換

1. 系統判斷目前下一場球局是否已經結束。
2. 若已超過每週三 21:30，系統自動建立或切換到下一週三 19:30-21:30 的場次。
3. 新場次的報名名單從空白開始。
4. 前一週場次與報名資料保留在資料庫。

## 7. 頁面需求

### 7.1 首頁

首頁目的：快速確認本週能不能報名。

首頁內容：

- 網站名稱：Wednesday Basketball
- 下一場日期與時間
- 地點：木柵國中
- 正式人數：`目前 / 15`
- 候補人數：`目前 / 5`
- 場次狀態：
  - 可報名
  - 正式已滿，可候補
  - 本週已滿
  - 本週報名已截止
- 主要按鈕：「查看 / 報名」

首頁不顯示完整名單，避免資訊太擠，尤其是手機畫面。

### 7.2 詳情頁

詳情頁目的：完成報名、查看名單、取消報名。

詳情頁內容：

- 下一場球局資訊
- 正式名單區
- 候補名單區
- 報名表單
- 滿額狀態提示

正式名單：

- 顯示最多 15 人。
- 每個名字旁顯示取消按鈕。
- 建議顯示順位，例如 `1. Kevin`。

候補名單：

- 顯示最多 5 人。
- 每個名字旁顯示取消按鈕。
- 建議顯示候補順位，例如 `候補 1`。

報名表單：

- 單一輸入欄位：名字。
- 送出按鈕：報名。
- 若正式名單未滿，按鈕文字可顯示「加入正式名單」。
- 若正式名單已滿但候補未滿，按鈕文字可顯示「加入候補」。
- 若總人數已滿，按鈕停用並顯示「本週已滿」。
- 若已超過星期三 12:00，按鈕停用並顯示「本週報名已截止」。

### 7.3 管理後台

管理後台目的：讓管理者處理臨時狀況，例如有人在群組臨時說要來、有人請管理者幫忙取消、或需要調整候補順位。

管理後台需有簡單權限保護：

- 第一版可使用管理密碼或 admin token。
- 未通過驗證者不可進入後台。
- 管理權限只提供給固定管理者，不開放一般球友。

管理後台內容：

- 下一場球局資訊。
- 正式名單。
- 候補名單。
- 取消名單。
- 目前正式人數、候補人數與剩餘名額。

管理者可執行：

- 手動新增報名者。
- 取消報名者。
- 將候補調整為正式。
- 將正式調整為候補。
- 調整名單順序。
- 查看已取消紀錄。

管理後台限制：

- 不直接刪除資料，取消或調整都應更新狀態並保留紀錄。
- 若正式名單已滿，管理者手動加入正式名單時需顯示提醒。
- 若候補名單已滿，管理者手動加入候補時需顯示提醒。
- 管理者可以覆蓋一般使用者端的報名截止限制。

## 8. 手機版設計要求

多數球友會用手機開啟，因此第一版必須採手機優先設計。

### 8.1 手機優先

- 首頁第一個畫面內要看得到日期、時間、地點、人數狀態與報名入口。
- 詳情頁要優先顯示報名狀態與報名表單。
- 名單可以往下滑，但人數狀態要清楚。
- 按鈕高度要適合手指點擊，建議至少 44px。
- 名字與取消按鈕不可太靠近，避免誤觸。

### 8.2 視覺風格

- 語言：中文為主。
- 風格：簡單、有運動感、清楚直接。
- 可以使用籃球相關色彩或視覺元素，但不應影響可讀性。
- 避免過度裝飾，重點是快速完成報名。

### 8.3 響應式

- 手機版為主要體驗。
- 桌機版可以使用較寬的內容容器，但不需要做複雜 dashboard。
- 正式名單與候補名單在手機上可上下排列。
- 桌機版可以並排顯示正式名單與候補名單。

## 9. 資料模型

### 9.1 `events`

| 欄位 | 型別 | 說明 |
| --- | --- | --- |
| `id` | UUID / integer | 場次 ID |
| `date` | date | 場次日期 |
| `weekday` | string | 星期，例如 Wednesday |
| `start_time` | time | 開始時間，固定 19:30 |
| `end_time` | time | 結束時間，固定 21:30 |
| `location` | string | 地點，固定木柵國中 |
| `confirmed_capacity` | integer | 正式名額，固定 15 |
| `waitlist_capacity` | integer | 候補名額，固定 5 |
| `registration_deadline` | datetime | 報名截止時間，固定為場次當天 12:00 |
| `created_at` | datetime | 建立時間 |
| `updated_at` | datetime | 更新時間 |

### 9.2 `registrations`

| 欄位 | 型別 | 說明 |
| --- | --- | --- |
| `id` | UUID / integer | 報名 ID |
| `event_id` | UUID / integer | 對應場次 ID |
| `name` | string | 報名者名字 |
| `status` | enum | `confirmed`, `waitlisted`, `cancelled` |
| `position` | integer | 報名順位，用於排序與候補遞補 |
| `created_by` | enum | `player`, `admin` |
| `updated_by` | enum | `player`, `admin`, nullable |
| `created_at` | datetime | 報名時間 |
| `updated_at` | datetime | 更新時間 |

資料限制：

- 同一 `event_id` 下，未取消的相同 `name` 不可重複。
- 已取消資料不刪除，以保留歷史紀錄。
- 名單排序以 `created_at` 或 `position` 為準。

## 10. 狀態規則

### 10.1 場次狀態

- `open`：正式名單未滿 15 人，可加入正式名單。
- `waitlist_open`：正式名單已滿，候補名單未滿，可加入候補。
- `full`：正式名單與候補名單都已滿，不可報名。
- `closed`：已超過星期三 12:00 報名截止時間，不可由球友端報名。
- `ended`：本週場次已結束，系統應切換到下一週場次。

### 10.2 報名狀態

- `confirmed`：正式名單。
- `waitlisted`：候補名單。
- `cancelled`：已取消。

### 10.3 遞補規則

當一位 `confirmed` 使用者取消：

1. 系統找到同場次最早報名的 `waitlisted` 使用者。
2. 將該使用者狀態改為 `confirmed`。
3. 更新正式名單與候補名單顯示。
4. 若沒有候補者，正式名單空出一個名額。

## 11. 錯誤與提示訊息

常見提示：

- 報名成功，你已加入正式名單。
- 正式名單已滿，你已加入候補。
- 這個名字已經報名了。
- 本週已滿。
- 本週報名已截止。
- 已取消報名。
- 候補已自動遞補。
- 管理者已更新名單。

錯誤訊息要短、清楚，適合手機畫面閱讀。

## 12. 成功指標

- 使用者可以在 30 秒內完成報名。
- 使用者一打開首頁就能知道本週是否還有名額。
- 同一場次不會出現同名重複報名。
- 正式 15 人與候補 5 人規則正確運作。
- 正式名單取消後，候補能自動遞補。
- 每週三 12:00 後，球友端不可再報名。
- 管理者可以在後台調整正式名單與候補名單。
- 每週三 21:30 後能自動切換到下一週場次。
- 歷史資料能正確保存。

## 13. 第一版驗收條件

- 首頁顯示下一場：每週三 19:30-21:30，木柵國中。
- 首頁只顯示人數摘要，不顯示完整名單。
- 詳情頁顯示正式名單與候補名單。
- 使用者只輸入名字即可報名。
- 正式名單未滿時，報名者進入正式名單。
- 正式名單滿 15 人後，報名者進入候補名單。
- 候補名單滿 5 人後，報名功能關閉。
- 星期三 12:00 後，球友端報名功能關閉。
- 同名不可重複報名同一場次。
- 每個名字旁都有取消按鈕。
- 取消正式名單成員後，候補第一位自動補上。
- 管理者後台需要權限保護。
- 管理者可以新增、取消、調整正式與候補名單。
- 場次結束後，自動切換到下一週。
- 資料庫保存每週場次與報名紀錄。

## 14. 後續可能擴充

- LINE 通知或 LINE Login。
- 匯出歷史報名紀錄。
- 顯示歷史出席統計。
- 加入取消保護，例如報名代碼或 PIN。
- 臨時停賽或更改場地公告。
